<h1>Editing Book</h1>
<% if @book.errors.any? %>
<%= @book.errors.count %>errors prohidited this book from being saved:
<ul>
  <% @book.errors.full_messages.each do |message| %>
  <li><%= message %></li>
  <% end %>
</ul>
<% end %>

<%= form_with model:@book, url: update_list_path(@book.id), method: :patch do |f| %>
<div>Title</div>
  <%= f.text_field:title %>
<div>Body</div>
  <%= f.text_area:body %>

  <%= f.submit'Update Book' %>

  <% end %>

<%= link_to "Show",book_path(@book.id) %>

  <%= link_to "Back","/books" %>